import React from "react";
import ReactRouge from "./ReactRouge";

const App = () => (
    <div className="App">
        <ReactRouge width={80} height={40} tileSize={16} />
    </div>
);

export default App;
